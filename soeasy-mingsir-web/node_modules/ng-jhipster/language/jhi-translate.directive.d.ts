import { ElementRef, OnChanges, OnInit } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';
import { JhiConfigService } from '../config.service';
/**
 * A wrapper directive on top of the translate pipe as the inbuilt translate directive from ngx-translate is too verbose and buggy
 */
export declare class JhiTranslateDirective implements OnChanges, OnInit {
    private configService;
    private el;
    private translateService;
    jhiTranslate: string;
    translateValues: any;
    constructor(configService: JhiConfigService, el: ElementRef, translateService: TranslateService);
    ngOnInit(): void;
    ngOnChanges(): void;
    private getTranslation;
}
